@model InSelfLove.Web.ViewModels.Article.ArticlesPaginationViewModel
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer localizer

@{
    // Provide title to layout partial for title header
    ViewData["Title"] = @localizer["Articles"].Value;
}

<partial name="_SlimShowcasePartial" model="@ViewData["Title"]" />

@*_ArticlesAllPartial exists because it's used by 2 controllers - Articles & Search *@
<partial name="_ArticlesAllPartial" model="@Model" />
